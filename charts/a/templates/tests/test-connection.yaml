apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "a.fullname" . }}-test-connection"
  labels:
    {{- include "a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
